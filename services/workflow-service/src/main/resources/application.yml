server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: custoking-workflow-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
    hikari:
      data-source-properties:
        currentSchema: ${WORKFLOW_DB_SCHEMA:public}
  flyway:
    enabled: ${WORKFLOW_FLYWAY_ENABLED:true}
    schemas: ${WORKFLOW_DB_SCHEMA:public}
    default-schema: ${WORKFLOW_DB_SCHEMA:public}
    table: flyway_schema_history

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: ${MANAGEMENT_HEALTH_SHOW_DETAILS:always}
      probes:
        enabled: true

workflow:
  read-token: ${WORKFLOW_READ_TOKEN:}
  db:
    schema: ${WORKFLOW_DB_SCHEMA:public}
